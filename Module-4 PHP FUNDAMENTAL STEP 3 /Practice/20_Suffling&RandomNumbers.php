<?php

/**
 * Shuffling arrays & random numbers:
 * 
 * mt_rand()        ->mt_rand() is a built-in PHP function that generates a random integer using the Mersenne Twister algorithm. It produces a random integer between a specified minimum and maximum value.
 *                    Syntax: mt_rand(int $min, int $max): int
 * 
 * shuffle()        ->shuffle() is a built-in PHP function that randomly shuffles the order of elements in an array. It takes an array as its parameter and modifies the original array in place, so there is no need to assign the result of shuffle() to a new variable. 
 *                    Syntam: shuffle($arrayName);
 *  
 * 
 * 
 */

echo "20. Discussion of shuffling arrays and random numbers: \n";


?>
