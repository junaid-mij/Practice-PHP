<?php

/**
 * Difference type of Utility Function:
 * array_walk()         ->it provide square value in array.
 *                        Note(to use it we have call a callBack_function name 'square' inside the walk function & it doesn't return any value)
 *                        Syntx-> array_walk($arrayName,'square');
 * 
 * array_map()          ->it provide cube value in array.
 *                        Note(to use it we have call a callBack_function name 'cube' inside the walk function & it return value)
 *                        Syntx-> array_mape('cube',$arrayName);  
 *   
 * array_filter()       ->it provide filtering value in array. As an example we can filter out even or odd number || name etc.
 *                        Note(to use it we have call a callBack_function name 'even/odd/name/etc' inside the walk function & it return value)
 *                        Syntx-> array_filter($arrayName,'even/odd/name/etc');  
 * 
 */

echo "16. Array Utility Functions - Very Important - Walk, Map and Filter: \n";


?>