<?php

/**
 * Listing Function:Array
 * 
 * list()           ->list() is a language construct in PHP that assigns variables as if they were an array. It allows you to assign values to a list of variables in a single statement based on the position of values in an array or iterable object.
 *                    Syntax-> list($var1, $var2, $var3, ...) = $array;
 * 
 * 
 */

echo "18. Using the list function to get data from an array into a variable: \n";


?>
